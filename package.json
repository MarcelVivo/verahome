{
  "name": "vera-home-web",
  "version": "1.0.0",
  "private": true,
  "description": "Vera Home Website und Vera Portal mit Capacitor-iOS-Testhülle.",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-capacitor-web.js",
    "ios:add": "npm run mobile:prepare && npx cap add ios",
    "ios:sync": "npm run mobile:prepare && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npx cap run ios",
    "cap:doctor": "npx cap doctor"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
